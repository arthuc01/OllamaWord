/**
 * This project uses a ribbon button with ShowTaskpane, so no command function
 * is needed yet. The file exists because Office manifests commonly reference a
 * function file for ribbon integration, and it gives you a clear place to add
 * command handlers later.
 */

Office.onReady(() => {
  // Intentionally empty.
});
